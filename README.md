# Trans-Search

## 文件结构

```
trans-search/
├── backend/
│   ├── main.py            # 后端
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example       # 复制为 .env
├── frontend/
│   └── index.html         # 静态前端
├── scripts/
│   └── seed.py            # 示例数据录入脚本
└── docker-compose.yml     # 一键启动 Qdrant + 后端
```
